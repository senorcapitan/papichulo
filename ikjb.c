#include <stdio.h>
int TOTALPOINTSEARNED;
int TOTALPOINTSPOSSIBLE;
int a;
int main(void) {
int assignments = scanf("how many different assignments?\n");
int gradesEarned[assignments];
int pointsPossible[assignments];
}
{
  int numbers[assignments];
  for(int i = 1; i<= assignments; i++){
    numbers[i]=i
  } printf("points earned for assignment[i]\n");

for(int i=1; i<=assignments;i++){
  printf("points possible for assignment[i]\n");
}
}
